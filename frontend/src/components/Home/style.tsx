import { COLORS } from "../utils/utils"

export const HomeStyle = () => {
  return {
    height: '100%',
    fontFamily: 'Arial, Helvetica, sans-serif'
  }

}

export const NavbarBackground = () => {
  return { backgroundColor: COLORS.red2 }
}