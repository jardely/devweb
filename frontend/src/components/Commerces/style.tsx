import { COLORS } from "../utils/utils"

export const CommerceStyle = () => {
  return {
    boxShadow: '0 3px 9px 0 rgba(61, 62, 64, 0.2)',
    borderRadius: '5px',
    backgroundColor: COLORS.branco
  }
}